export default function HomePage(): React.JSX.Element {
  return (
    <main className="login-shell">
      <section className="login-card">
        <span className="eyebrow">AMAN/DMAN</span>
        <h1>Configuration Service</h1>
        <p>This service hosts operational configuration APIs and an internal admin interface.</p>
      </section>
    </main>
  );
}
